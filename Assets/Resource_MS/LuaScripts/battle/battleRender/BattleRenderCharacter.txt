---
--- Generated by EmmyLua(https://github.com/EmmyLua)
--- Created by liuyubao.
--- DateTime: 2021/12/30 15:52
---@class BattleRenderCharacter
local BattleRenderCharacter = SimpleClassUtil:class()

function BattleRenderCharacter:initialize(data)

    self._go_model = data.go_model
    ---@type string
    self._index = data.index

    self._heroId = data.heroId

    self._uid = data.uid

    self:init()

end

function BattleRenderCharacter:getUid()
    return self._uid
end

function BattleRenderCharacter:init()
    self:initDebugComponent()
    self:initAnimationComponent()
end

function BattleRenderCharacter:initDebugComponent()
    if GameStatic_Battle.General_Grizmo then
        self._gizmosCircle = self._go_model.transform:GetComponent("GameObjectGizmosCircleLine")
    end
end
---animation
function BattleRenderCharacter:initAnimationComponent()

    self._animator = self._go_model.transform:GetComponent("Animator")

    self._aniState = Enum_Battle.AniState.idle

    self._ani_speed = 1
end

function BattleRenderCharacter:getAnimationComponent()
    return {
        animator =  self._animator,
        aniState = self._aniState,
        ani_speed = self._ani_speed
    }
end

function BattleRenderCharacter:setAnimationComponent(aniState,ani_speed)
    if aniState then
        self._aniState = aniState
    end

    if ani_speed then
        self._ani_speed = ani_speed
    end
end

function BattleRenderCharacter:setPosition(newPos)
    if newPos then
        self._go_model.transform.position = newPos
    end
end

function BattleRenderCharacter:setGizmosCircle(volume_r,place_r,dislocation_r,detection_r,attack_r,place_r_base)
    if GameStatic_Battle.General_Grizmo and self._gizmosCircle then
        self._gizmosCircle:BeginDraw(volume_r,place_r,dislocation_r,detection_r,attack_r,place_r_base)
    end
end

function BattleRenderCharacter:setRotation(newVec)
    if newVec and self._go_model and self._go_model.transform then
        self._go_model.transform.localEulerAngles = newVec
    end
end

function BattleRenderCharacter:setScaleZ(z)
    if z and self._go_model and self._go_model.transform then
        self._go_model.transform:SetLocalScaleZ(z);
    end
end

return BattleRenderCharacter