---
--- Generated by EmmyLua(https://github.com/EmmyLua)
--- Created by liuyubao.
--- DateTime: 2021/12/30 14:13
---@class bt_ac_initSkillCd
local bt_ac_initSkillCd = SimpleClassUtil:class(IAction)

function bt_ac_initSkillCd:initData(data)

end
---@param blackBoard BlackBoard_BattleBase
function bt_ac_initSkillCd:doAction(blackBoard,agent,dt)
    if blackBoard and blackBoard._skillData  then
        for i, skillData in pairs(blackBoard._skillData ) do
            if skillData and skillData._skill_time_initCd then

            end
        end
    end
    return Enum_BT.Node_ResultType.Success
end

return bt_ac_initSkillCd