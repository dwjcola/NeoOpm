---
--- Generated by EmmyLua(https://github.com/EmmyLua)
--- Created by lyb.
--- DateTime: 2021/12/8 1:02
---  取反修饰节点 Inverter        对子节点执行结果取反
---@class NodeDecoratorInverter : NodeDecorator
local NodeDecoratorInverter = SimpleClassUtil:class(NodeDecorator)

function NodeDecoratorInverter:initialize()
    self.super:initialize(Enum_BT.Node_Type.Decorator_Inverter)
    self._runningNode = 0
end

function NodeDecoratorInverter:onEnter()
    self.super:onEnter()
    self._runningNode = 0
end

function NodeDecoratorInverter:onExit()
    self.super:onExit()
    for i = 1, self:getChildesCount() do
        local value = 1 << i
        if (self._runningNode & value) > 0 then
            local node = self:getChildesByIndex(i)
            node:postPosition(Enum_BT.Node_ResultType.Fail)
        end
    end
end


function NodeDecoratorInverter:execute(blackboard,contextManager)
    self:log_enter("NodeDecoratorInverter    Entity: ",self:getEntityId())
    local resultType = Enum_BT.Node_ResultType.Fail
    local count = #self:getChildes()
    for i = 1, count do
        local nodeBase = self:getChildesByIndex(i)

        nodeBase:prePosition()
        resultType = nodeBase:execute(blackboard,contextManager)
        nodeBase:postPosition(resultType)

        if resultType == Enum_BT.Node_ResultType.Running then
            self._runningNode =  self._runningNode | (1 << i)
        end
    end
    if resultType == Enum_BT.Node_ResultType.Fail then
        return Enum_BT.Node_ResultType.Success
    elseif resultType == Enum_BT.Node_ResultType.Success then
        return Enum_BT.Node_ResultType.Fail
    else
        return resultType
    end
    self:log("NodeDecoratorInverter  Entity: ",self:getEntityId(),  "  resultType :", resultType)
    return resultType
end


return NodeDecoratorInverter