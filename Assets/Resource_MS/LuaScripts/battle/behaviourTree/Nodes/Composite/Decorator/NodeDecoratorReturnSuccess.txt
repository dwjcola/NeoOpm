---
--- Generated by EmmyLua(https://github.com/EmmyLua)
--- Created by lyb.
--- DateTime: 2021/12/8 1:02
---  修饰节点_返回Success
---       执行节点，无论节点返回 Success、Fail、Running 执行结束后永远向父节点返回 Success
---
---@class NodeDecoratorReturnSuccess : NodeDecoratorReturnConst
local NodeDecoratorReturnSuccess = SimpleClassUtil:class(NodeDecoratorReturnConst)

function NodeDecoratorReturnSuccess:initialize()
    self.super:initialize(Enum_BT.Node_Type.Decorator_Return_Success)
    self._runningNode = 0
end

function NodeDecoratorReturnSuccess:onEnter()
    self.super:onEnter()
    self._runningNode = 0
end

function NodeDecoratorReturnSuccess:onExit()
    self.super:onExit()
    for i = 1, self:getChildesCount() do
        local value = 1 << i
        if (self._runningNode & value) > 0 then
            local node = self:getChildesByIndex(i)
            node:postPosition(Enum_BT.Node_ResultType.Fail)
        end
    end
end

function NodeDecoratorReturnSuccess:execute(blackboard,contextManager)
    local resultType = Enum_BT.Node_ResultType.Fail
    local count = #self:getChildes()
    for i = 1, count do
        local nodeBase = self:getChildesByIndex(i)

        nodeBase:prePosition()
        resultType = nodeBase:execute(blackboard,contextManager)
        nodeBase:postPosition(resultType)

        if resultType == Enum_BT.Node_ResultType.Running then
            self._runningNode =  self._runningNode | (1 << i)
        end
    end
    resultType = Enum_BT.Node_ResultType.Success
    self:log("NodeDecoratorReturnSuccess    Entity: ",self:getEntityId(),    "  resultType :", resultType)
    return resultType
end


return NodeDecoratorReturnSuccess